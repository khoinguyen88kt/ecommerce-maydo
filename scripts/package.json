{
  "name": "suit-layer-generator",
  "version": "1.0.0",
  "type": "module",
  "description": "Generate 2D layer images from 3D GLB models",
  "scripts": {
    "generate": "node scripts/generate-layers.js",
    "generate:batch": "node scripts/batch-generate.js"
  },
  "dependencies": {
    "three": "^0.160.0",
    "canvas": "^2.11.2",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0"
  }
}